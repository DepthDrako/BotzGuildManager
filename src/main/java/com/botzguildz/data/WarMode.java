package com.botzguildz.data;

public enum WarMode {
    OPEN_WORLD,  // PvP enabled between guilds anywhere in the world
    ARENA        // Both guilds teleported to the guild war dimension (requires ARENA_WAR upgrade)
}
